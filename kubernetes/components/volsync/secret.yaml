# SPDX-FileCopyrightText: 2026 Sofie <sofie+git@mailbox.org>
#
# SPDX-License-Identifier: 0BSD

apiVersion: v1
kind: Secret
metadata:
  name: volsync-${APP}
stringData:
  RESTIC_REPOSITORY: ${SECRET_S3_ENDPOINT}/volsync-${APP}
  RESTIC_PASSWORD: ${SECRET_PASSWORD}
  AWS_ACCESS_KEY_ID: ${SECRET_AWS_ACCESS_KEY_ID}
  AWS_SECRET_ACCESS_KEY: ${SECRET_AWS_SECRET_ACCESS_KEY}
